# LINE連携 AIクッキー画像生成バックエンド 実装タスク

## 1. 事前準備（プロジェクト土台）
- [x] Firebase Cloud Functions（Node.js 20+ / TypeScript）のプロジェクト初期化を行う。
- [x] 必要ライブラリを追加する（LINE SDK、Firebase Admin SDK、google-genai、HTTPクライアントなど）。
- [x] 環境変数を定義する（LINEチャネルシークレット、LINEチャネルアクセストークン、GenAI設定、Firebase設定）。
- [x] ローカル実行方法（Emulator / Docker）とデプロイ手順を README に最小限記載する。

## 2. Webhookエンドポイント実装
- [x] `POST /webhook`（または同等の関数エントリ）を作成する。
- [x] `X-Line-Signature` を使って署名検証を実装する。
- [x] 署名検証失敗時に `401` を返す処理を実装する。
- [x] LINEイベントのうち `message` のみ処理対象にする分岐を実装する。
- [x] `text` メッセージ以外は安全にスキップする。

## 3. 入力テキスト対応（プロンプト内で禁止用語対応）
- [x] 禁止用語の厳密判定はコードで行わず、生成AIへの指示で対応する方針を明記する。
- [x] プロンプトに「放送禁止用語や不適切表現の場合は、くま型のお菓子を生成する」ルールを追加する。
- [x] 通常入力時はユーザー入力の内容をモチーフ化する既存ルールを維持する。

## 4. 画像生成プロンプト作成
- [x] 仕様のテンプレートをベースにプロンプト生成関数を作る。
- [x] 「文字そのものを書かない」「背景透過（transparent background）」「中央配置」など必須条件を固定文として含める。
- [x] 画像アスペクト比 `1:1` を生成リクエストに必ず反映する。
- [x] 出力形式が `PNG` になるように生成条件へ明記する。

## 5. Gemini画像生成連携
- [x] `gemini-flash-2.5-image` 呼び出し処理を実装する。
- [x] 生成結果から画像データ（URLまたはバイナリ）を取得する処理を実装する。
- [x] API失敗・タイムアウト時の例外処理を実装する。

## 6. Firebase Storage保存
- [ ] 生成画像を一時データから取得してアップロード可能な形式に変換する。
- [ ] `/cookies/{timestamp}_{user_id}.png` 形式で保存する。
- [ ] 透過情報（アルファチャンネル）を保持したまま保存できることを確認する。
- [ ] `user_id` は LINE `source.userId` をそのまま使用する。
- [ ] 保存後に公開ダウンロードURLを取得する。
- [ ] URLを恒久公開として扱える設定を確認する。

## 7. Realtime Database登録
- [x] 保存先 `cookies` 直下に `push()` でレコード追加する。
- [x] 保存データに `imageUrl` / `text` / `createdAt` / `posX` / `posY` / `rotation` / `scale` を含める。
- [x] `posX` は `0.0 ~ 1.0` の乱数で生成する。
- [x] `posY` は `0.0 ~ 1.0` の乱数で生成する。
- [x] `rotation` は `-20 ~ +20` の乱数で生成する。
- [x] `scale` は `0.8 ~ 1.2` の乱数で生成する。

## 8. LINE返信処理
- [ ] 成功時に「つくったよ」テキストを `reply` する。
- [ ] 成功時に生成した画像を `reply` する（テキストと同一返信内）。
- [ ] 失敗時にエラーメッセージを `reply` する。
- [ ] 返信トークン期限切れなどの失敗時ログを残す。

## 9. エラーハンドリングとログ
- [ ] 処理全体を段階ごとに `try/except` で保護する。
- [ ] どの段階で失敗したか分かる構造化ログを出す。
- [ ] ユーザー向けメッセージと内部ログ詳細を分離する（内部情報を返信しない）。

## 10. 動作確認（最小テスト）
- [ ] 正常系：テキスト送信で画像生成→Storage保存→DB登録→LINE返信を確認する。
- [ ] 異常系：署名不正で `401` が返ることを確認する。
- [ ] 異常系：生成API失敗時にエラー返信されることを確認する。
- [ ] 放送禁止用語・不適切表現入力で「くまの形」生成になることを確認する。
- [ ] DBに `posX` / `rotation` が範囲内で保存されることを確認する。

## 11. デプロイと運用準備
- [ ] `firebase deploy --only functions` でCloud Functionsへデプロイする。
- [ ] LINE Developers側にWebhook URLを設定し、疎通確認する。
- [ ] 本番用の環境変数・シークレット設定を確認する。
- [ ] 最低限の監視項目（エラー率、失敗ログ）を決める。

## 12. 将来改善（任意）
- [ ] モデレーション精度向上のため、将来的に入力分類（安全性判定）を追加検討する。
- [ ] 生成失敗時の再試行戦略を導入する。
- [ ] 画像最適化（容量削減）を追加する。
- [ ] テスト自動化（ユニットテスト/結合テスト）を追加する。

## 13. 白背景の透過化（sharp後処理）
- [x] `functions` に `sharp` を依存追加する。
- [x] 画像後処理モジュール（例: `services/imagePostProcess.ts`）を作成する（※別Cloud Functionは作らない）。
- [x] 「白に近いピクセルを透過（alpha=0）」へ変換するロジックを実装する。
- [x] 透過しきい値（例: RGB >= 245）を定数化し、調整しやすくする。
- [x] 透過化後の出力を `PNG` で統一する。
- [x] `index.ts` の同一 `webhook` フローに後処理ステージを追加する（`generate -> postprocess -> upload`）。
- [x] ログに後処理ステージ（入力サイズ/出力サイズ/透過適用）を追加する。
- [x] 後処理失敗時はフォールバック方針を決める（失敗で中断 or 元画像を保存）。
- [ ] 動作確認: 白背景画像で透過されることを目視確認する。
- [ ] 動作確認: 透過済み画像がStorage/LINE/Web表示で崩れないことを確認する。
